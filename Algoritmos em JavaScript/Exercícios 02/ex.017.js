var basemaior = 9
var basemenor = 5
var altura = 9
if (basemaior > 0 && basemenor > 0) {
    console.log(`A área do trapézio é ${(basemaior + basemenor) * altura/2}`)   
} else {
    console.log('A base menor e a base maior devem ser maiores que 0.')
}
